<style>
  .hero-section {
    background: var(--primary-gradient);
    min-height: 100vh;
    color: white;
    display: flex;
    align-items: center;
    padding: 40px 20px;
    position: relative;
    overflow: hidden;
  }

  .hero-section::before {
    content: "";
    position: absolute;
    top: -50%;
    right: -10%;
    width: 600px;
    height: 600px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 50%;
    animation: float-blob 6s ease-in-out infinite;
  }

  @keyframes float-blob {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(30px); }
  }

  .hero-content { position: relative; z-index: 2; }

  .hero-title {
    font-size: 3.8rem;
    font-weight: 800;
    margin-bottom: 1.5rem;
    line-height: 1.1;
  }

  .hero-subtitle {
    font-size: 1.3rem;
    opacity: 0.95;
    margin-bottom: 2.5rem;
    line-height: 1.6;
    max-width: 600px;
  }

  .hero-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    padding: 1.1rem 2.8rem;
    font-size: 1.1rem;
    font-weight: 700;
    background: white;
    color: var(--primary-color);
    border-radius: 50px;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
  }

  .hero-cta:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.3);
  }

  .features-section { padding: 100px 0; }

  .section-title {
    font-size: 2.5rem;
    font-weight: 800;
    text-align: center;
    margin-bottom: 1rem;
    background: var(--primary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .section-subtitle {
    text-align: center;
    color: #718096;
    font-size: 1.1rem;
    margin-bottom: 4rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  [data-bs-theme="dark"] .section-subtitle { color: #b0b0b0; }

  .feature-card {
    background: white;
    border-radius: 20px;
    padding: 2.5rem;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
    border: 2px solid transparent;
  }

  [data-bs-theme="dark"] .feature-card {
    background: rgba(50, 50, 80, 0.8);
    box-shadow: 0 8px 20px rgba(102, 126, 234, 0.2);
  }

  .feature-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 16px 40px rgba(102, 126, 234, 0.15);
  }

  .feature-icon {
    font-size: 3rem;
    background: var(--primary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: 1.5rem;
  }

  .feature-card h3 {
    font-weight: 700;
    color: #2d3748;
    margin-bottom: 1rem;
    font-size: 1.3rem;
  }

  [data-bs-theme="dark"] .feature-card h3 { color: #e0e0e0; }

  .feature-card p {
    color: #718096;
    line-height: 1.6;
    margin: 0;
  }

  [data-bs-theme="dark"] .feature-card p { color: #b0b0b0; }

  .cta-section {
    background: var(--primary-gradient);
    padding: 100px 0;
    color: white;
    text-align: center;
  }

  .cta-section h2 {
    font-size: 2.5rem;
    font-weight: 800;
    margin-bottom: 1.5rem;
  }

  .cta-section p {
    font-size: 1.2rem;
    opacity: 0.95;
    margin-bottom: 2.5rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    padding: 1.1rem 2.8rem;
    background: white;
    color: var(--primary-color);
    border-radius: 50px;
    font-weight: 700;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
  }

  .cta-btn:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.3);
  }
</style>

<section class="hero-section">
  <div class="container">
    <div class="hero-content">
      <h1 class="hero-title">Never Forget to Follow Up</h1>
      <p class="hero-subtitle">Schedule smart email reminders and stay on top of your communications. Never miss a deadline or important message again.</p>
      <a href="/schedule" class="hero-cta"><i class="bi bi-calendar-plus"></i>Get Started Now</a>
    </div>
  </div>
</section>

<section class="features-section">
  <div class="container">
    <h2 class="section-title">Why Choose RemindMe</h2>
    <p class="section-subtitle">Perfect for students managing assignments, deadlines, and collaborations</p>
    <div class="row g-4">
      <div class="col-md-6 col-lg-4">
        <div class="feature-card">
          <i class="bi bi-lightning-charge feature-icon"></i>
          <h3>Lightning Fast</h3>
          <p>Set up reminders in seconds. No complicated steps, just simple and instant.</p>
        </div>
      </div>
      <div class="col-md-6 col-lg-4">
        <div class="feature-card">
          <i class="bi bi-shield-check feature-icon"></i>
          <h3>Reliable & Secure</h3>
          <p>Your data is safe. Automatic reminders you can always count on.</p>
        </div>
      </div>
      <div class="col-md-6 col-lg-4">
        <div class="feature-card">
          <i class="bi bi-graph-up feature-icon"></i>
          <h3>Stay Productive</h3>
          <p>Boost productivity by never forgetting important tasks and deadlines.</p>
        </div>
      </div>
      <div class="col-md-6 col-lg-4">
        <div class="feature-card">
          <i class="bi bi-clock-history feature-icon"></i>
          <h3>Track Everything</h3>
          <p>See all your scheduled reminders in one organized view.</p>
        </div>
      </div>
      <div class="col-md-6 col-lg-4">
        <div class="feature-card">
          <i class="bi bi-bell-fill feature-icon"></i>
          <h3>Instant Notifications</h3>
          <p>Get reminded exactly when you need it, straight to your inbox.</p>
        </div>
      </div>
      <div class="col-md-6 col-lg-4">
        <div class="feature-card">
          <i class="bi bi-phone feature-icon"></i>
          <h3>Always Available</h3>
          <p>Access your reminders anytime, anywhere on any device.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="cta-section">
  <div class="container">
    <h2>Ready to Never Miss a Deadline?</h2>
    <p>Start scheduling your first reminder today and revolutionize how you manage your time</p>
    <a href="/schedule" class="cta-btn"><i class="bi bi-arrow-right"></i>Schedule Your First Reminder</a>
  </div>
</section>